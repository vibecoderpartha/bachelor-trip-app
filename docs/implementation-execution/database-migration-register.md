# Database Migration Register

No migration file is created by this Draft package. `DBM-*` identifiers are
stable planning units, not SQL filenames. A unit has one owning IR; a later
packet may consume its evidence but cannot take ownership. Schema, data, policy,
Storage, Realtime, and generated-type work are deliberately separated.

| ID | Owning IR / Wave | Purpose and affected records | Dependency and order | Constraint/index and policy requirement | Transform / compatibility expectation | Generated-type timing | Rollback and verification | Status |
|---|---|---|---|---|---|---|---|---|
| DBM-001 | IR-002 / W1 | Tenant root: `groups`, `profiles`, and stable `group_members` identity. A Participant is `group_members.id`; a separate `participants` table is not planned. | After IR-001 capability decision; before every Group-owned schema unit. | One Group parent for every member; stable member identity; same-Group parent paths and indexes for lookups. No policy activation before this structure exists. | Additive target foundation; legacy rows remain source evidence. | Refresh after the approved unit is applied to the isolated target and provenance is captured. | Schema rollback only if no dependent target data exists; otherwise forward repair. Verify parent constraints and two-Group fixture setup. | Draft |
| DBM-002 | IR-002 / W1 | One effective Group configuration, archive state, and audit foundations. | After DBM-001; before bootstrap/configuration/feature units. | Group uniqueness; date/order, IANA timezone, ISO currency, archive/audit indexes. | Additive; configuration values are not inferred from UI state. | Refresh after approved application. | Do not remove configuration referenced by a Group; forward repair after data exists. Verify constraints and archive-safe reads/writes. | Draft |
| DBM-003 | IR-004 / W2 | Profile-to-Auth linkage and profile privacy support. | DBM-001 before policy unit DBM-006. | Auth-user uniqueness and profile lookup index; no client-supplied identity authority. | Profiles add presentation only and do not create membership. | Refresh after approved application. | Preserve Profiles on failed policy rollout; repair forward if linkage is populated. Verify Auth-derived actor and privacy denial. | Draft |
| DBM-004 | IR-005 / W3 | Membership, Invitation, Owner/Member lifecycle, and lifecycle audit structures. | DBM-001/002 and DBM-006 policy foundation; before claims, Storage, Realtime, and Active Group consumers. | One active relationship per Auth User/Group, Invitation state/expiry/recipient indexes, ownership-continuity enforcement support. | Existing inactive/unclaimed identities remain valid history; no automatic authority. | Refresh after approved application. | A lifecycle rollback must not create an ownerless Group or revive an invalid Invitation. Verify last-Owner, replay, and archived cases. | Draft |
| DBM-005 | IR-006 / W3 | Legacy Participant provenance and claim/attachment constraints. | DBM-004 and DBM-006; a legacy-data input for DBM-011, not a prerequisite for creating new finance schema. | Immutable stable participant identity; claim uniqueness and source-map/audit indexes. | Unclaimed is the safe compatible state; no name, PIN, Profile, or Invitation proof. | Refresh after approved application. | Never delete or rewrite referenced participant history to undo a claim; deactivate/quarantine or forward-correct. Verify conflict, retry, and no-Owner-promotion results. | Draft |
| DBM-006 | IR-007 / W2 | Base deny-by-default RLS policy units for Profiles, Groups, configuration, and membership primitives. | Only after DBM-001–003 support the policy predicates; before trusted-operation and later-table policy units. | Predicate-supporting indexes; session actor, active membership, role, and archive checks. | No permissive interval and no Active Group/client predicate. Later tables add their own policies only after their schemas exist. | Refresh after approved policy/schema changes if generated types include affected definitions. | Do not revert to permissive policies. Hold target inaccessible and forward-correct a failed policy. Verify two-account/two-Group matrix and substitution denial. | Draft |
| DBM-007 | IR-009 / W3 | Group-scoped travel-document metadata and private Storage authorization support. | DBM-004/006 before document product work; before DBM-012 activation sequencing. | Object-metadata Group/Event consistency and lookup indexes; private-object policy prerequisites. | Public URL/path never becomes target authority; source objects are transform inputs only. | Refresh after approved metadata schema. | Object and metadata are reconciled/quarantined, never independently deleted as a rollback shortcut. Verify private read and cross-Group object denial. | Draft |
| DBM-008 | IR-012 / W4 | Group Events, audience relationships, and Participant-owned Todos. | DBM-001/004/006 and Active Group boundary; before DBM-011 data transforms. | Group/Participant/Event same-Group constraints and query indexes. Audience remains presentation, not confidentiality. | Legacy name arrays and `user_name` are mapped only by reviewed transforms. | Refresh after approved application. | Preserve source mapping; forward-correct after target data. Verify inactive/archive and cross-Group denial. | Draft |
| DBM-009 | IR-014 / W4 | Normalized Expenses, payer contributions, final shares, Settlements, and immutable accounting/FX evidence. | DBM-001/006; consumes stable member identity. DBM-005 is required only for legacy-row transforms, not for the new schema unit. | Exact amount/check/reconciliation constraints; same-Group foreign keys; immutable history and deterministic-order indexes. | No float/name/JSON authority; complete finance sets transform atomically or quarantine. | Refresh after approved application. | Data-bearing finance work is not generally reversible; preserve history and use controlled forward repair. Verify EQ-01, SET-01, USD-01 and reconciliation. | Draft |
| DBM-010 | IR-015 / W4 | Configuration extensions and FX-snapshot fields needed for destination, reference-FX, and accounting separation. | DBM-002/006; before DBM-011 configuration/FX transform. | Valid display-context and FX-evidence indexes; accounting-currency history cannot be silently changed. | Extends DBM-002 rather than creating a second configuration authority; no live-rate rewrite of history. | Refresh after approved application. | Do not roll history back by recalculation; forward-correct metadata. Verify Bali/non-Bali and fallback cases. | Draft |
| DBM-011 | IR-016 / W5 | Replay-safe data transform delivery for M01–M18. | DBM-001–005 and DBM-007–010 destination constraints first. | No new product schema; source-map, manifest, checksum, and quarantine records must be sufficient to prove one logical unit. | Source-preserving, idempotent transform only; unknowns stay excepted/quarantined. | Refresh only if a separately approved schema correction is required; a transform alone does not change types. | Never claim data transforms are reversible. Retain source/manifest, quarantine partial output, and use an approved retry or forward repair. Verify counts, checksums, and per-map reconciliation. | Draft |
| DBM-012 | IR-016 / W5 | Migration-stage activation sequencing for already-owned Storage/private-object and Realtime/publication controls. | DBM-006/007/008 and their security cases first; no activation before supporting schema/policies pass. | No new product policy model. Activation must preserve current membership checks and be testable against existing indexes. | Security transition is staged only after source/object/query manifest evidence; no global/public compatibility interval. | Refresh after any approved supporting schema/policy change, not merely because a publication changes. | Failed activation leaves the target inaccessible or delivery disabled; never restore public/global access. Verify private-object, stale-membership, and two-Group Realtime denial. | Draft |
| DBM-013 | IR-002 / W1 | Generated database-type provenance and refresh checkpoint. This is an operational DBM unit, not an SQL migration. | After each approved schema or type-affecting policy/API unit; first target baseline follows DBM-001/002, then refresh serially after DBM-003–010. | Generated output must identify source schema/version and must not substitute for RLS tests. | No generated file is currently present; target path remains Proposed until the approved generator chooses one. | Refresh after, never before, the corresponding approved schema change; review diffs before typed consumers proceed. | Regenerate from the prior approved source or remove an unconsumed artifact; never hand-edit generated output. Verify provenance and type-check capability. | Draft |

## Ordering controls

- DBM-001–005 and DBM-007–010 are schema/data-model planning units.
- DBM-006 is the base policy unit; DBM-012 only sequences migration-stage
  activation after Storage, Realtime, and supporting policies are independently
  ready.
- DBM-011 is data transformation, not a substitute for destination constraints.
- DBM-013 is a serial generated-type refresh checkpoint. It cannot run ahead of
  schema provenance or mask a failed migration/policy review.

No timestamp convention is assigned: the legacy numbered migration directory
does not establish a safe target naming rule.
